import 'package:flutter/material.dart';

class AddPersonalChannel extends StatelessWidget {
  const AddPersonalChannel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
